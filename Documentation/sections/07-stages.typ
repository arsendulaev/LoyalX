#import "../data.typ": data
#import "../list.typ": *

= СТАДИИ И ЭТАПЫ РАЗРАБОТКИ

== Стадии разработки, этапы и содержание работ

#h(2.8em)Стадии и этапы разработки были выявлены с учетом ГОСТ 19.102-77.

#let column_names = (
  [*Стадия\\ разработки*],
  [*Этап работ*],
  [*Содержание работ*],
  [*Сроки\\ выполнения*],
)

#table(
  columns: (3fr, 3fr, 4fr, auto),
  rows: (15mm, auto),
  align: center + horizon,
  stroke: 0.5pt + gray.lighten(50%),
  table.header(..column_names),

  // ============================================================
  // ТЕХНИЧЕСКОЕ ЗАДАНИЕ
  // ============================================================
  table.cell(
    rowspan: 2,
    align: horizon,
    rotate(0deg, reflow: true)[*Техническое задание*],
  ),
  table.cell(
    rowspan: 2,
    align: horizon,
    rotate(0deg, reflow: true)[Исследование и проектирование],
  ),
  align(left)[
    1) Анализ предметной области (Web3 лояльность), выбор стандарта токенов (Jetton), проектирование архитектуры смарт-контрактов.
  ],
  [01.11.25 --\ 23.11.25],

  align(left)[
    2) Разработка и утверждение ТЗ, выбор стека (Tact, React, TON Connect).
  ],
  [24.11.25 --\ 16.12.25],

  // ============================================================
  // РАБОЧИЙ ПРОЕКТ
  // ============================================================
  table.cell(
    rowspan: 6,
    align: horizon,
    rotate(0deg, reflow: true)[*Рабочий проект*],
  ),
  table.cell(
    rowspan: 3,
    align: horizon,
    rotate(0deg, reflow: true)[Разработка ПО],
  ),
  align(left)[
    1) Разработка смарт-контрактов на Tact: Factory, Jetton Minter, Swap Router. Написание тестов в Blueprint.
  ],
  [01.01.26 --\ 20.01.26],

  align(left)[
    2) Разработка Frontend (Telegram Mini App): экраны кошелька, сканера, обмена. Интеграция TON Connect.
  ],
  [21.01.26 --\ 10.02.26],

  align(left)[
    3) Реализация Backend и Indexer: кеширование данных блокчейна, уведомления, Telegram Bot API.
  ],
  [11.02.26 --\ 25.02.26],

  table.cell(
    rowspan: 1,
    align: horizon,
    rotate(0deg, reflow: true)[Интеграция],
  ),
  align(left)[
    1) Развертывание контрактов в Testnet, связка фронтенда с контрактами, тестирование E2E сценариев (регистрация бренда -> покупка -> обмен).
  ],
  [26.02.26 --\ 05.03.26],

  table.cell(
    rowspan: 1,
    align: horizon,
    rotate(0deg, reflow: true)[Испытания и сдача],
  ),
  align(left)[
    1) Проведение функционального тестирования, оформление документации, подготовка к защите.
  ],
  [06.03.26 --\ 15.03.26],

  table.cell(
    rowspan: 1,
    align: horizon,
    rotate(0deg, reflow: true)[*Внедрение*],
  ),
  align(left)[
    Публикация в Mainnet и демонстрация работы.
  ],
  [16.03.26],
)

== Сроки разработки и исполнители

#v(0.4em)
#list2[
Программный продукт должен быть завершён не позднее 23.05.26.
Исполнитель -- А.А. Дулаев.
]